#include<stdio.h>
#include<math.h>
int main()
{
    int a[100],n,i,max;
    printf(" enter a number  :");
    scanf("%d",&n);
    max=a[0];
     if(max<a[i])
     max=a[i];
     max=(max <a[i])?a[i]:max;
     max=fmax(a[i],max);


     return 0;
}
